# Delta DV Gluten Stack Notes - 2026-05-23

Base native reader:
- Apache PR #12040 is merged.
- Local merged base commit: `7bb00d163 [VL] Delta: Add native Delta DV reader support (#12040)`.

## Stack 1 - JVM Delta DV Scan PR

Primary branch to push:
- `split/delta-dv-java-scan-pr`
- Current local head: `cabfaf3c6 Revert "Format ANSI analysis script"`
- Worktree used for validation: `incubator-gluten-dv-java-scan-review-clean`
- Status: clean.

Scope:
- JVM-side Delta DV scan handoff.
- Delta 3.3 and Delta 4.0 only.
- Delta 2.4 shim removed.
- No DELETE command.

Size from #12040 base:
- 28 files changed.
- 2031 insertions, 131 deletions.

Validation:
- Spark 3.5 / Delta 3.3 `VeloxDeltaSuite`: 19/19 passed.
- Spark 4.0 / Delta 4.0 `VeloxDeltaSuite`: 19/19 passed.

Review note:
- This is shippable as the current follow-up PR if reviewers are okay with a 2k-line scan handoff diff.
- If reviewers ask for smaller slices, use the alternate smaller stack below.

## Alternate Smaller Scan Stack

Payload transport:
- Branch: `split/delta-dv-payload-transport-pr`
- Head: `2d36b582f [VL][Delta] Add materialized DV payload transport`
- Size from #12040 base: 15 files, 496 insertions, 21 deletions.

Scan handoff:
- Branch: `split/delta-dv-java-scan-handoff-pr`
- Head: `15373adf6 [VL][Delta] Add JVM Delta DV scan handoff`
- Size from payload branch: 13 files, 1535 insertions, 110 deletions.

Scan hardening and benchmark:
- Branch: `split/delta-dv-scan-hardening-benchmark-pr`
- Head: `f9daab869 [VL][Delta] Keep scan hardening independent of planning API`
- Size from scan handoff branch: 9 files, 861 insertions, 49 deletions.

## Stack 2 - Scan Hardening And Benchmark

Branch:
- `split/delta-dv-scan-hardening-benchmark-pr`
- Current local head: `f9daab869 [VL][Delta] Keep scan hardening independent of planning API`
- Worktree: `incubator-gluten-dv-scan-hardening`
- Status: clean.

Scope:
- Broader DV scan correctness coverage.
- Benchmark harness for DV read path.
- No generic planning API changes.
- No DELETE command.

Validation:
- Spark 3.5 / Delta 3.3 `VeloxDeltaSuite`: 24/24 passed.
- Spark 4.0 / Delta 4.0 `VeloxDeltaSuite`: 24/24 passed.
- `git diff --check`: clean.

Local scan-only benchmark:
- Rows: 5,000,000.
- Files: 16.
- Delete modulo: 10.
- Iterations: 5.
- Warmups: 2.
- Mode: `count`.
- Spark median: planning 168.5 ms, execution 201.3 ms, total 367.8 ms.
- Gluten median: planning 173.9 ms, execution 93.6 ms, total 275.6 ms.
- Speedup: planning 0.97x, execution 2.15x, total 1.33x.

Read-path posture:
- Scan execution path demonstrates >2x.
- Full wall-clock is not 2x because planning and non-scan overhead still count in total time.

## Stack 3 - Generic Planning Tuning

Branch:
- `split/gluten-planning-tuning-pr`
- Current local head: `d9b135a87 [VL][Delta] Reduce Delta planning validation overhead`
- Worktree: `incubator-gluten-planning-tuning-pr`
- Status: clean.

Scope:
- Delta-specific planning validation tuning.
- Replaces the generic Delta component transform with a single Delta transform pass.
- Validates only Delta scan/project/filter candidates and skips already converted Gluten nodes.
- Keeps candidate checks next to the Delta offload rules for easier rebase after scan handoff changes.

Size from current `upstream/main`:
- 4 files changed.
- 73 insertions, 10 deletions.

Validation:
- Spark 3.5 / Delta 3.3 export-only compile: passed.
- Spark 4.0 / Delta 4.0 export-only compile: passed.
- `git diff --check`: clean.

Ship note:
- Keep this separate from the DV scan PR.
- This is Delta-specific and can be reviewed as a small planning-performance follow-up.

## Stack 3b - Native Delta Bitmap Support

Branch:
- `split/delta-dv-native-bitmap-pr`
- Current local head: `c431d4d76 [VL][Delta] Add native Delta bitmap aggregation support`
- Worktree: `incubator-gluten-dv-native-bitmap-pr`
- Status: clean.

Scope:
- Delta-compatible `RoaringBitmapArray` native representation.
- Native `bitmapaggregator` aggregate function.
- Substrait aggregate mapping for portable Delta bitmap aggregation.
- Focused C++ unit-test coverage for merge/cardinality/last.

Size from current `upstream/main`:
- 9 files changed.
- 354 insertions, 11 deletions.

Validation:
- Spark 3.5 / Delta 3.3 export-only compile: passed.
- Spark 4.0 / Delta 4.0 export-only compile: passed.
- `git diff --check`: clean.
- `clang-format --dry-run -Werror` on changed C++ files: passed with local clang-format 21.

Ship note:
- This is independent from DELETE command integration.
- A local native C++ rebuild/test is still needed before opening as a PR.

## Stack 4 - DV DELETE Staging

Branch:
- `stage/delta-dv-delete-tests-benchmarks-20260523`
- Current local head: `d4fcbee96 [VL][Delta] Stage DV DELETE tests and benchmarks`
- Worktree: `incubator-gluten-1-dv-delete`
- Status: clean.

Staged layers:
- `5337e195d [VL][Delta] Stage DV DELETE native bitmap support`
- `e4a4d84f7 [VL][Delta] Stage DV DELETE target scan helpers`
- `43435d45e [VL][Delta] Stage DV DELETE command integration`
- `d4fcbee96 [VL][Delta] Stage DV DELETE tests and benchmarks`

Size from current delete base:
- 33 files changed.
- 9807 insertions, 97 deletions.

Ship note:
- Do not ship this as one PR.
- Rebase/restack after scan handoff and scan hardening settle.
- Split into native bitmap support, DML row-index/target scan helpers, DELETE command integration, and tests/benchmarks.

DELETE posture:
- Progress is preserved in staged commits.
- Correctness coverage is broad but must be revalidated after rebase.
- Performance work is not final; commit/checksum floor and bitmap/materialization path remain the main areas.
